{% extends 'base.html.twig' %}

{% block title %}{{ title }} | {{ parent() }}{% endblock %}

{% block body %}
    <p>{{ title|upper }}</p>
    {# comment me #}
    <ul>
    {% for track in tracks %}
        <li>{{ track.song }} {{ track.artist}} </li>
    {% endfor %}
{% endblock %}
